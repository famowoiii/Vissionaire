# Performance Optimization Settings for CPU-only

# Skip frames - process 1 frame every N frames
FRAME_SKIP=5

# Reduce image size before inference
INPUT_SIZE=320

# Use lighter model
MODEL_SIZE=n

# Disable some detections to speed up
SKIP_TRACKING=true

# Batch size (lower = faster on CPU)
BATCH_SIZE=1
